import { WorkaroundMatFormAccessorDirectiveDirective } from './workaround-mat-form-accessor.directive';

describe('WorkaroundMatFormAccessorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new WorkaroundMatFormAccessorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});

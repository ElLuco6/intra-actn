import { EricdateFormatPipe } from './ericdate-format.pipe';

describe('EricdateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new EricdateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});

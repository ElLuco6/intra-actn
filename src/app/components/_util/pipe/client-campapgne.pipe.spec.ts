import { ClientProspectFournisseurPipe } from './client-campapgne.pipe';

describe('ClientCampapgnePipe', () => {
  it('create an instance', () => {
    const pipe = new ClientProspectFournisseurPipe();
    expect(pipe).toBeTruthy();
  });
});

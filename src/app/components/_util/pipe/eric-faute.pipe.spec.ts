import { EricFautePipe } from './eric-faute.pipe';

describe('EricFautePipe', () => {
  it('create an instance', () => {
    const pipe = new EricFautePipe();
    expect(pipe).toBeTruthy();
  });
});
